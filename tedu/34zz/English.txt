over	在...上面
run		运行
control	控制
state	状态


盾牌随机出现

英雄机添加无敌状态：碰到盾牌后切换无敌状态、无敌动画、

销毁动画、销毁动画结束后无敌失效、无敌状态时碰到敌机后

英雄机不死但敌机会死。

游戏结束之后闪屏

原有的step方法只能区分正常和销毁两种状态

----------------------
盾牌是一个单独的对象

添加无敌状态为对hero构造方法的修改：
参数需要变：无敌状态帧、销毁无敌状态帧

属性需要变：添加飞机当前状态属性、添加飞机下一状态属性、添加无敌时间属性(每射击一次时间减少1)
定义一个对象字面量表示飞机当前状态
（当状态需要改变时不会 立即改变，而是等状态改变时需要完成的工作结束后才会切换状态）、
英雄级包含以下三个状态：正常、无敌状态、死亡

添加方法：checkState方法比较当前状态和下一状态，对不同组合情况作出不同响应。
	修改状态方法、变为无敌状态、取消无敌状态，当状态发生改变时需同时改变当前使用的图片
	
修改方法：hit，无敌状态时与敌飞机发生撞击无效，但与增益发生撞击有效；无敌时间可以延长即撞击到盾牌课叠加
完成这个修改可以修改父类构造方法的hit方法，也可以修改checkHit方法，知识修改checkHit方法的话需要修改的地方比较多
重写父类对象的step方法，在step方法里调用checkState方法

当事件发生时只需要去修改currentState和nextState的值，在checkState方法里会做不同的事













